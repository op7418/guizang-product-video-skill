import React from 'react';
// Technical fixture, not a claim about the target product. Replace with the feature's real
// components (client-mounted inside ProductContext) and remove the placeholder marker.
export function FeatureVisual() {
  return <div className="neutral-fixture" data-skill-placeholder="true">
    <div className="fx-label">COMPONENT ADAPTER</div>
    <h2>把真实组件放到这里</h2>
    <p>接入产品组件后，用影片时钟驱动它的真实状态。</p>
  </div>;
}
