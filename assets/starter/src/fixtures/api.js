// Fixture responses keyed by pathname. Derive data from the product's own catalogs/constants
// where possible (import them) so names, icons and options stay real. Never copy user data.
export const API_PREFIXES = ['/api/'];
export const API_FIXTURES = {
  // '/api/providers/models': {groups: [...]},
};
